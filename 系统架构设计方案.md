# AI 智能自媒体系统 · 系统架构设计方案

**文档版本**：1.0  
**编制日期**：2026-04-04  
**文档状态**：评审中  
**参考文档**：AI 智能自媒体系统需求说明书 v1.0

---

## 目录

1. [引言](#1-引言)
2. [架构设计原则](#2-架构设计原则)
3. [整体架构设计](#3-整体架构设计)
4. [技术栈选型](#4-技术栈选型)
5. [前端架构设计](#5-前端架构设计)
6. [后端微服务架构设计](#6-后端微服务架构设计)
7. [AI 引擎层设计](#7-ai-引擎层设计)
8. [数据架构设计](#8-数据架构设计)
9. [消息队列与事件驱动设计](#9-消息队列与事件驱动设计)
10. [API 网关与接口设计](#10-api-网关与接口设计)
11. [第三方平台集成设计](#11-第三方平台集成设计)
12. [安全架构设计](#12-安全架构设计)
13. [部署架构设计](#13-部署架构设计)
14. [高可用与容灾设计](#14-高可用与容灾设计)
15. [性能优化设计](#15-性能优化设计)
16. [监控与可观测性设计](#16-监控与可观测性设计)
17. [架构演进路线](#17-架构演进路线)

---

## 1 引言

### 1.1 编写目的

本文档基于《AI 智能自媒体系统需求说明书》，对系统整体架构进行设计，明确系统的技术分层、微服务划分、数据流向、AI 引擎集成方案、部署拓扑及非功能保障机制，为开发团队提供系统建设的技术蓝图。

### 1.2 设计范围

- 系统整体分层架构及各层职责划分
- 微服务划分与服务间通信机制
- AI 大模型接入与编排方案
- 数据存储选型与分层策略
- 多平台集成适配器设计
- 安全、高可用、可观测性等非功能架构设计

### 1.3 核心挑战

| 挑战 | 说明 |
|------|------|
| AI 多模型协同 | 需同时接入 GPT、Gemini、文心一言、通义千问等多个大模型，实现故障自动切换与效果对比 |
| 多平台适配 | 需适配公众号、小红书、抖音、视频号、快手、B 站等 6+ 平台，接口规范差异大 |
| 实时数据处理 | 多平台数据实时抓取聚合，5000+ 并发用户同时在线，数据量级达 PB 级 |
| 内容合规检测 | 内容生成→发布全链路需实时合规检测，延迟要求 ≤2 秒 |
| 自动化工作流 | 需支持用户自定义触发规则与复杂自动化流程的灵活编排 |

---

## 2 架构设计原则

| 原则 | 说明 |
|------|------|
| **模块化解耦** | 各业务模块独立部署、独立扩展，通过标准接口通信，模块间依赖最小化 |
| **AI 优先** | AI 能力内聚于独立引擎层，通过统一网关接入多模型，业务层无感知切换 |
| **数据驱动** | 所有业务行为产生数据埋点，数据流经统一管道反哺各模块决策优化 |
| **平台无关** | 第三方平台集成通过适配器模式封装，新平台接入只需实现适配器接口 |
| **高可用优先** | 核心链路（内容发布、数据采集）采用多活设计，单点故障不影响主流程 |
| **安全合规** | 安全机制贯穿全链路，数据加密、权限隔离、内容审核作为基础能力内置 |
| **渐进演进** | 采用分阶段架构演进，优先交付 MVP，再逐步扩展能力 |

---

## 3 整体架构设计

### 3.1 整体分层架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                           客户端层                                   │
│         Web (React)  │  H5/小程序  │  移动端 App (React Native)      │
└────────────────────────────┬────────────────────────────────────────┘
                             │ HTTPS / WebSocket
┌────────────────────────────▼────────────────────────────────────────┐
│                      API 网关层（Kong / Nginx）                      │
│   路由转发 │ 认证鉴权 │ 限流熔断 │ 协议转换 │ 日志追踪              │
└──┬──────────────┬──────────────┬──────────────┬──────────────┬──────┘
   │              │              │              │              │
┌──▼──┐  ┌────────▼───┐  ┌──────▼────┐  ┌─────▼─────┐  ┌────▼─────┐
│BFF  │  │ 选题内容   │  │ 用户画像  │  │ 数据分析  │  │  其他    │
│聚合 │  │ 微服务     │  │ 微服务    │  │ 微服务    │  │ 微服务   │
│层   │  │            │  │           │  │           │  │          │
└──┬──┘  └────────────┘  └───────────┘  └───────────┘  └──────────┘
   │
   │          应用服务层（Spring Cloud / Go 微服务）
   │  ┌──────────────────────────────────────────────────────────┐
   └─►│  内容生成服务 │ 发布调度服务 │ 变现服务 │ 监控预警服务   │
      │  用户互动服务 │ 素材管理服务 │ 工作流服务 │ 团队协作服务  │
      └──────────────────────────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│                      AI 引擎层                                       │
│  ┌──────────────┐  ┌─────────────┐  ┌──────────────┐  ┌──────────┐ │
│  │ 模型网关     │  │ Prompt 管理 │  │ 内容检测     │  │ 预测分析 │ │
│  │ (多模型路由) │  │ (模板引擎)  │  │ (合规/原创)  │  │ (ML)     │ │
│  └──────────────┘  └─────────────┘  └──────────────┘  └──────────┘ │
└────────────────────────────┬────────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│                     外部 AI 模型层                                   │
│    GPT-4o │ Gemini Pro │ 文心一言 │ 通义千问 │ 图像生成模型         │
└────────────────────────────┬────────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│                     数据存储层                                       │
│  MySQL(业务) │ MongoDB(内容) │ Redis(缓存) │ ClickHouse(分析)        │
│  Elasticsearch(搜索) │ MinIO/OSS(文件) │ Kafka(消息流)              │
└────────────────────────────┬────────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│                   第三方平台集成层                                   │
│  抖音 │ 小红书 │ 公众号 │ 视频号 │ 快手 │ B站 │ 企微 │ 电商平台   │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 数据流总览

```
用户操作
    │
    ▼
API 网关（认证 + 路由）
    │
    ├──→ BFF 聚合层（前端数据聚合与裁剪）
    │
    ├──→ 业务微服务（同步请求：CRUD、配置、查询）
    │         │
    │         └──→ AI 引擎层（内容生成、分析）
    │                   │
    │                   └──→ 外部大模型 API
    │
    └──→ 消息队列（异步任务：发布调度、数据采集、预警推送）
              │
              ├──→ 第三方平台集成层（发布内容、抓取数据）
              │
              └──→ 数据处理管道（ClickHouse 实时写入、ML 训练）
```

---

## 4 技术栈选型

### 4.1 前端技术栈

| 层次 | 技术选型 | 选型理由 |
|------|----------|----------|
| Web 框架 | React 18 + TypeScript | 生态成熟，组件复用性强，适合复杂数据看板 |
| 状态管理 | Zustand + React Query | 轻量高效，React Query 专为服务端状态设计 |
| UI 组件库 | Ant Design 5.x | 企业级组件丰富，表单/表格/图表覆盖全 |
| 数据可视化 | ECharts 5 + AntV G6 | ECharts 满足图表需求，G6 用于流程图/关系图 |
| 移动端 | React Native + Expo | 代码复用率高，支持 H5/小程序双端 |
| 小程序 | Taro 3（多端框架）| 一套代码编译微信/抖音/支付宝小程序 |
| 构建工具 | Vite 5 | 开发热更新极快，生产构建性能优异 |
| 实时通信 | WebSocket（Socket.IO）| 数据看板实时推送、消息通知 |

### 4.2 后端技术栈

| 层次 | 技术选型 | 选型理由 |
|------|----------|----------|
| 主框架 | Java 21 + Spring Boot 3 | 微服务生态完整，企业级稳定性高 |
| 微服务治理 | Spring Cloud Alibaba | Nacos（注册/配置）+ Sentinel（熔断限流）|
| 工作流引擎 | Flowable 7 | 支持 BPMN 2.0，灵活编排用户自定义自动化规则 |
| 定时任务 | XXL-Job | 分布式任务调度，支持 Cron 与数据驱动触发 |
| 对象存储 | MinIO / 阿里云 OSS | 素材库（图片/视频/音频）海量文件存储 |
| 搜索引擎 | Elasticsearch 8 | 素材全文检索、选题库检索 |
| 高性能网关 | Go（独立 Go 服务）| 平台数据抓取适配器，高并发 HTTP 请求性能优 |

### 4.3 AI 引擎技术栈

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| LLM 接入框架 | LangChain4j / LlamaIndex | 统一封装多模型调用，支持 Chain/Agent 编排 |
| Prompt 管理 | 自研 Prompt 模板引擎 | 支持变量插值、版本管理、效果评分 |
| 向量数据库 | Milvus 2 | 素材语义检索、用户兴趣向量存储 |
| 内容检测 | 自研 + 第三方 API | 阿里云内容安全 + 自研敏感词引擎 |
| 机器学习 | Python（FastAPI + scikit-learn/PyTorch）| 数据预测分析、用户分层模型 |
| 图像生成 | DALL·E 3 / Stable Diffusion API | 封面图、分镜图生成 |
| 语音合成 | 微软 Azure TTS / 阿里云语音合成 | 智能配音 |

### 4.4 数据存储技术栈

| 存储类型 | 选型 | 用途 |
|----------|------|------|
| 关系型数据库 | MySQL 8 + ShardingSphere | 用户账号、权限、商单、订单等核心业务数据 |
| 文档数据库 | MongoDB 7 | 内容草稿、素材元数据、用户画像标签 |
| 缓存 | Redis Cluster 7 | 会话、热点数据、分布式锁、排行榜 |
| 列式数据库 | ClickHouse | 多平台统计数据实时分析，PB 级数据支撑 |
| 消息队列 | Apache Kafka | 平台数据采集流、内容发布事件、异步通知 |
| 搜索引擎 | Elasticsearch 8 | 全文检索、日志分析 |
| 向量数据库 | Milvus 2 | 语义相似度检索 |
| 时序数据库 | InfluxDB | 系统监控指标、账号数据时序趋势 |

### 4.5 基础设施与 DevOps

| 类别 | 选型 |
|------|------|
| 容器化 | Docker + Kubernetes（K8s）|
| CI/CD | GitHub Actions + ArgoCD |
| 服务网格 | Istio（流量管理、可观测性）|
| 配置中心 | Nacos 2 |
| 链路追踪 | SkyWalking |
| 日志系统 | ELK（Elasticsearch + Logstash + Kibana）|
| 指标监控 | Prometheus + Grafana |
| 云平台 | 阿里云（国内）/ AWS（海外扩展）|

---

## 5 前端架构设计

### 5.1 前端分层架构

```
┌─────────────────────────────────────────────────────────┐
│                     页面层 (Pages)                       │
│  选题工作台 │ 内容创作台 │ 数据看板 │ 用户运营 │ 变现中心 │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   业务组件层 (Features)                  │
│  AI 生成面板 │ 内容编辑器 │ 图表组件 │ 任务看板 │ 表单    │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   基础组件层 (UI Components)             │
│               Ant Design + 自定义业务组件库              │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   数据层 (Data Layer)                    │
│  React Query（服务端状态）│ Zustand（本地状态）           │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   API 请求层 (Service)                   │
│        Axios 实例封装 │ WebSocket 实例 │ 请求拦截器       │
└─────────────────────────────────────────────────────────┘
```

### 5.2 关键页面模块

| 页面模块 | 核心功能 | 关键技术点 |
|----------|----------|------------|
| 智能选题工作台 | 热点抓取展示、选题评分、日历管理 | 实时数据推送（WebSocket）、拖拽日历（DnD） |
| AI 内容创作台 | 富文本编辑、AI 辅助生成、多版本管理 | 富文本编辑器（Tiptap）、流式 SSE 输出 |
| 数据可视化看板 | 多平台数据聚合展示、自定义布局 | ECharts 响应式图表、栅格拖拽布局 |
| 用户运营面板 | 用户分层、触达任务配置 | 树形标签选择器、任务规则可视化配置 |
| 发布调度中心 | 批量定时发布、跨平台状态管理 | 甘特图排期、实时发布状态轮询 |
| 变现中心 | 商单管理、变现收入统计 | 看板式流程追踪、收入趋势预测图 |

### 5.3 AI 内容生成流式交互

AI 内容生成采用 **Server-Sent Events（SSE）** 实现流式输出，用户可实时看到内容逐字生成，提升体验：

```
前端 → POST /api/content/generate（携带 prompt 参数）
     ← SSE 流式响应（分 token 逐步返回内容）
     ← SSE event: done（生成完成，返回完整元数据）
```

---

## 6 后端微服务架构设计

### 6.1 微服务划分

系统按业务领域划分为以下微服务：

```
┌────────────────────────────────────────────────────────────────────┐
│                       业务微服务群                                  │
├──────────────┬──────────────┬──────────────┬────────────────────────┤
│ 账号定位服务  │ 选题引擎服务  │ 内容生成服务  │ 素材管理服务           │
│ (account-    │ (topic-      │ (content-    │ (material-svc)         │
│  position)   │  engine)     │  gen)        │                        │
├──────────────┼──────────────┼──────────────┼────────────────────────┤
│ 用户画像服务  │ 用户触达服务  │ 智能互动服务  │ 私域沉淀服务           │
│ (user-       │ (reach-svc)  │ (interaction │ (private-domain)       │
│  profile)    │              │  -svc)       │                        │
├──────────────┼──────────────┼──────────────┼────────────────────────┤
│ 数据采集服务  │ 数据分析服务  │ 预警决策服务  │ 竞品监控服务           │
│ (data-       │ (analytics-  │ (alert-svc)  │ (competitor-svc)       │
│  collector)  │  engine)     │              │                        │
├──────────────┼──────────────┼──────────────┼────────────────────────┤
│ 内容分发服务  │ 发布调度服务  │ 投放辅助服务  │ 变现服务               │
│ (distribute  │ (publish-    │ (ad-assist)  │ (monetize-svc)         │
│  -svc)       │  scheduler)  │              │                        │
├──────────────┼──────────────┼──────────────┼────────────────────────┤
│ 工作流服务    │ 团队协作服务  │ 通知推送服务  │ 配置管理服务           │
│ (workflow-   │ (collab-svc) │ (notify-svc) │ (config-svc)           │
│  svc)        │              │              │                        │
└──────────────┴──────────────┴──────────────┴────────────────────────┘
```

### 6.2 服务间通信策略

| 通信场景 | 协议/方式 | 说明 |
|----------|-----------|------|
| 同步强依赖调用 | gRPC（Protobuf）| 服务间内部调用，如内容生成调用 AI 引擎 |
| 异步解耦调用 | Kafka 消息队列 | 发布任务、数据采集、通知推送等异步场景 |
| 前端查询 | REST / GraphQL | BFF 层对前端提供聚合 API |
| 实时推送 | WebSocket | 数据看板实时更新、AI 内容生成流式输出 |
| 配置同步 | Nacos Config | 统一配置中心，配置变更实时推送 |

### 6.3 关键微服务详细设计

#### 6.3.1 选题引擎服务（topic-engine）

```
职责：多源数据抓取、选题评分、选题日历管理

核心组件：
├── DataCrawlerScheduler    # 定时爬取热点数据（XXL-Job 驱动）
├── TopicScoringEngine      # 选题四维度评分算法
│     ├── HeatScorer        # 传播热度评分
│     ├── CompetitionScorer # 竞争度评分
│     ├── MatchScorer       # 账号匹配度评分（调 AI 引擎）
│     └── CommercialScorer  # 商业潜力评分
├── TopicCalendarManager    # 选题日历 CRUD
├── ConflictDetector        # 选题冲突检测
└── TopicRepository         # 选题库持久化（MongoDB）

数据流：
外部热点 API → DataCrawler → Kafka(topic.raw) → TopicScoringEngine
                                                      │
                                                      ▼
                                               TopicCalendarManager
                                                      │
                                                      ▼
                                               MongoDB(topic_library)
```

#### 6.3.2 内容生成服务（content-gen）

```
职责：多模态内容生成、合规检测、多平台适配

核心组件：
├── ContentPipelineOrchestrator  # 内容生成流程编排（Flowable 工作流）
├── TextGenerationService        # 文本内容生成（调 AI 引擎）
├── ImageGenerationService       # 视觉内容生成（DALL·E/SD）
├── AudioVideoService            # 音视频生成（TTS + 剪辑接口）
├── ComplianceChecker            # 合规检测（敏感词 + 原创度 + 版权）
├── PlatformAdaptorManager       # 多平台内容格式适配
└── ContentVersionManager        # 内容多版本管理（MongoDB）

内容生成工作流（Flowable BPMN）：
选题确认 → 大纲生成 → 初稿生成 → AI润色 → 合规检测
    │                                          │
    │                                    [违规] → 修改建议
    │                                    [通过] → 多平台适配 → 定稿入队
    │
    └──（发布后）→ 数据采集 → 迭代建议生成
```

#### 6.3.3 数据采集服务（data-collector）

```
职责：多平台数据实时采集、清洗、聚合

核心组件：
├── PlatformConnectorPool    # 平台连接器池（各平台适配器实例）
├── DataFetchScheduler       # 数据抓取调度（实时 + 定时双模式）
├── DataCleaningPipeline     # 数据清洗与去重
├── DataAggregator           # 多平台数据统一聚合
└── AnomalyDetector          # 数据异常检测 → 触发预警事件

数据采集链路：
平台API → PlatformConnector(适配器) → Kafka(platform.data.raw)
              │                              │
              │                    DataCleaningPipeline
              │                              │
              │                    ClickHouse(analytics_db)
              │                              │
              └─────────────── AnomalyDetector → Kafka(alert.events)
```

#### 6.3.4 发布调度服务（publish-scheduler）

```
职责：跨平台批量定时发布、发布状态管理

核心组件：
├── PublishTaskManager       # 发布任务 CRUD
├── OptimalTimeRecommender   # 最优发布时间推荐算法
├── BatchPublishExecutor     # 批量发布执行器（XXL-Job + 平台适配器）
├── PublishStateTracker      # 发布状态实时追踪
└── PublishReviewReporter    # 发布效果复盘报告生成

发布流程：
用户设置排期 → PublishTaskManager 入库
                        │
                XXL-Job 定时触发
                        │
              BatchPublishExecutor
                        │
            ┌───────────┴────────────┐
            │    PlatformAdaptors    │
            │  (各平台发布接口调用)   │
            └───────────┬────────────┘
                        │
              PublishStateTracker → WebSocket 推送前端
```

### 6.4 BFF 聚合层设计

BFF（Backend For Frontend）层为不同端（Web、H5、小程序）提供定制化 API，避免前端多次请求，减少数据冗余：

```
┌─────────────────────────────────────────────┐
│              BFF 聚合层                      │
│                                             │
│  /api/v1/dashboard  → 聚合：数据采集 +       │
│                        分析 + 预警服务       │
│                                             │
│  /api/v1/workbench  → 聚合：选题 + 内容 +   │
│                        发布调度服务         │
│                                             │
│  /api/v1/users      → 聚合：用户画像 +       │
│                        触达 + 互动服务       │
│                                             │
│  /api/v1/monetize   → 聚合：变现 + 商单 +   │
│                        数据分析服务         │
└─────────────────────────────────────────────┘
```

---

## 7 AI 引擎层设计

### 7.1 AI 引擎架构

```
┌─────────────────────────────────────────────────────────────┐
│                      AI 引擎层                               │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │               模型网关（Model Gateway）              │   │
│  │  路由策略：按场景/负载/成本/质量动态路由到最优模型    │   │
│  │  故障转移：主模型超时/失败自动切换备用模型            │   │
│  │  限流熔断：按 Token 配额控制各模型调用频率            │   │
│  └───────────────────────┬─────────────────────────────┘   │
│                          │                                  │
│  ┌────────────┐  ┌────────▼──────┐  ┌───────────────────┐  │
│  │ Prompt     │  │  LLM Chain    │  │  Agent Executor   │  │
│  │ 模板引擎   │  │  编排引擎     │  │  (AutoAgent)      │  │
│  │            │  │  (LangChain4j)│  │  复杂任务自动规划  │  │
│  └────────────┘  └───────────────┘  └───────────────────┘  │
│                                                             │
│  ┌────────────┐  ┌───────────────┐  ┌───────────────────┐  │
│  │ 内容合规   │  │  原创度检测   │  │  数据预测分析     │  │
│  │ 检测引擎   │  │  多模型互检   │  │  (ML Pipeline)    │  │
│  └────────────┘  └───────────────┘  └───────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                          │
    ┌─────────────────────┼─────────────────────┐
    ▼                     ▼                     ▼
  GPT-4o             文心一言 /            图像生成
  Gemini Pro         通义千问              DALL·E 3
  Claude 3            （备用）             Stable Diffusion
```

### 7.2 多模型路由策略

| 场景 | 主模型 | 备用模型 | 路由依据 |
|------|--------|----------|----------|
| 长文本内容生成 | GPT-4o | Claude 3 / 文心一言 | 输出质量优先 |
| 短文案/标题生成 | GPT-4o-mini | 通义千问-turbo | 响应速度 + 成本 |
| 数据分析报告 | Gemini Pro | GPT-4o | 数据推理能力 |
| 中文营销文案 | 文心一言 4.0 | 通义千问 | 中文语境优化 |
| 图像生成 | DALL·E 3 | Stable Diffusion | 质量/成本均衡 |
| 语音合成 | Azure TTS | 阿里云 TTS | 音色丰富度 |
| 情绪分析（NLP）| 自研小模型（Fine-tune）| 通义千问 | 延迟 ≤500ms |

### 7.3 Prompt 模板引擎设计

```
Prompt 模板结构：
{
  "template_id": "topic_scoring_v2",
  "scene": "topic_analysis",
  "version": "2.1",
  "template": "你是一位自媒体运营专家...\n选题：{{topic}}\n账号定位：{{account_profile}}\n...",
  "variables": ["topic", "account_profile", "platform"],
  "model_config": {"temperature": 0.3, "max_tokens": 1000},
  "quality_score": 8.7,
  "usage_count": 15420
}

模板管理流程：
创建模板 → A/B 测试（与旧版本对比质量分）→ 灰度上线 → 全量发布
                │
           效果不佳 → 回滚上一版本
```

### 7.4 ML 预测分析管道

```
数据预测分析（Python FastAPI 微服务）：

历史数据（ClickHouse）
    │
    ▼
特征工程（Feature Engineering）
    │ 发布时间、内容类型、账号阶段、平台、互动率历史 etc.
    ▼
模型训练（定期离线训练）
    │ Time Series Forecasting（LSTM / Prophet）
    ▼
模型服务化（FastAPI + MLflow 模型注册）
    │
    ▼
预测结果 → 缓存 Redis（TTL=1h）→ 前端展示
```

---

## 8 数据架构设计

### 8.1 数据分层存储策略

```
┌───────────────────────────────────────────────────────────────┐
│                       数据分层架构                             │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  ODS 层（原始数据层）                                    │  │
│  │  Kafka 消息流：平台原始数据、用户行为事件、内容操作日志   │  │
│  └────────────────────────┬────────────────────────────────┘  │
│                           │ Flink 实时处理                    │
│  ┌────────────────────────▼────────────────────────────────┐  │
│  │  DWD 层（数据明细层）                                   │  │
│  │  ClickHouse：清洗后的明细事实数据，按平台/内容/用户分区   │  │
│  └────────────────────────┬────────────────────────────────┘  │
│                           │ 聚合计算（定时/实时）             │
│  ┌────────────────────────▼────────────────────────────────┐  │
│  │  DWS 层（数据汇总层）                                   │  │
│  │  ClickHouse 物化视图：日/周/月维度聚合，支持看板查询      │  │
│  └────────────────────────┬────────────────────────────────┘  │
│                           │                                   │
│  ┌────────────────────────▼────────────────────────────────┐  │
│  │  ADS 层（应用数据层）                                   │  │
│  │  Redis 缓存：高频看板指标（TTL=5min）                    │  │
│  │  MySQL：用户配置、商单、权限等核心业务数据               │  │
│  └─────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘
```

### 8.2 各存储组件数据模型

#### MySQL 核心表设计（业务数据）

```sql
-- 用户账号表
account (id, user_id, platform, platform_account_id, oauth_token,
         stage, domain, created_at, updated_at)

-- 内容表
content (id, account_id, topic_id, title, content_type, status,
         platform_versions JSON, publish_time, created_at)

-- 发布任务表
publish_task (id, content_id, platform, scheduled_time, status,
              platform_post_id, publish_result JSON)

-- 商单表
commercial_order (id, account_id, brand, type, amount, status,
                  start_time, end_time, settlement_status)

-- 用户团队权限表
team_member (id, team_id, user_id, role, permissions JSON)
```

#### MongoDB 文档设计（内容与画像数据）

```javascript
// 内容草稿文档
{
  _id: ObjectId,
  content_id: "uuid",
  versions: [
    { version: 1, text: "...", platform: "douyin", created_at: ISODate }
  ],
  ai_suggestions: [...],
  compliance_check: { status: "passed", score: 92, issues: [] }
}

// 用户画像文档
{
  _id: ObjectId,
  account_id: "uuid",
  basic_tags: { age_range: "18-24", gender: "female", region: "北京" },
  behavior_tags: { active_time: "20:00-22:00", content_pref: ["美妆", "护肤"] },
  value_tier: "core_fan",        // core_fan / potential / lost / casual
  last_updated: ISODate
}
```

#### ClickHouse 分析表设计

```sql
-- 内容数据明细表（按日期分区）
CREATE TABLE content_metrics (
    content_id   String,
    platform     LowCardinality(String),
    account_id   String,
    metric_date  Date,
    views        UInt64,
    likes        UInt64,
    comments     UInt64,
    shares       UInt64,
    conversions  UInt64,
    fans_gained  Int64
) ENGINE = MergeTree()
PARTITION BY toYYYYMM(metric_date)
ORDER BY (platform, account_id, metric_date);
```

### 8.3 数据一致性策略

| 场景 | 一致性方案 |
|------|----------|
| 内容状态变更（草稿→定稿→发布）| MySQL 事务 + 状态机 |
| 多平台发布结果聚合 | Saga 补偿事务（分布式）|
| 用户画像更新 | 最终一致性（Kafka + 异步处理）|
| 商单佣金结算 | 强一致性事务（两阶段提交）|
| 实时数据看板指标 | 允许短暂不一致（缓存 TTL 策略）|

---

## 9 消息队列与事件驱动设计

### 9.1 Kafka Topic 规划

| Topic 名称 | 生产者 | 消费者 | 说明 |
|-----------|--------|--------|------|
| `platform.data.raw` | 数据采集服务 | 数据清洗管道 | 各平台原始统计数据 |
| `platform.data.cleaned` | 数据清洗管道 | 数据分析服务、ClickHouse | 清洗后数据 |
| `content.publish.queue` | 发布调度服务 | 各平台发布适配器 | 内容发布任务队列 |
| `content.published` | 平台发布适配器 | 数据采集服务、监控服务 | 发布完成事件 |
| `alert.events` | 异常检测服务 | 通知推送服务 | 数据异常预警事件 |
| `user.behavior` | 各业务服务 | 用户画像服务 | 用户行为数据流 |
| `workflow.triggers` | 各业务服务 | 工作流服务 | 自动化规则触发事件 |
| `ai.task.queue` | 各业务服务 | AI 引擎层 | AI 异步任务队列 |

### 9.2 自动化工作流事件编排

用户自定义自动化规则通过 **Flowable 工作流引擎** 实现：

```
用户配置规则示例：
  触发条件：粉丝新增 ≥ 1000
  执行动作：自动发送欢迎私信

执行流程：
用户行为数据 → Kafka(user.behavior)
                       │
               工作流服务消费
                       │
               规则条件匹配引擎（CEP 复杂事件处理）
                       │
               [条件满足] → Flowable 流程实例启动
                       │
               动作执行节点（调用互动服务发送私信）
                       │
               执行结果记录 → 通知用户
```

---

## 10 API 网关与接口设计

### 10.1 API 网关功能设计

```
Kong 网关配置：

路由管理：
  /api/v1/topic/**     → topic-engine 服务
  /api/v1/content/**   → content-gen 服务
  /api/v1/analytics/** → analytics-engine 服务
  /api/v1/publish/**   → publish-scheduler 服务
  /api/v1/users/**     → user-profile 服务
  /api/v1/monetize/**  → monetize-svc 服务

插件配置：
  - JWT 验证（jwt plugin）
  - 限流（rate-limiting：1000 req/min/用户，10000 req/min/IP）
  - 熔断（circuit-breaker：错误率 >50% 触发熔断）
  - 请求日志（http-log → Kafka → ELK）
  - CORS（cors plugin）
  - 请求体大小限制（request-size-limiting：50MB，素材上传专用路由除外）
```

### 10.2 统一 API 响应规范

```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "traceId": "abc123-xxx",
  "timestamp": 1712204400000
}

错误响应：
{
  "code": 40001,
  "message": "选题内容与账号定位偏差超过阈值",
  "data": null,
  "traceId": "abc123-xxx"
}
```

### 10.3 核心 API 接口设计

#### 内容生成接口（SSE 流式）
```
POST /api/v1/content/generate
Request: { topic_id, platform, style, word_count }
Response: SSE Stream
  event: chunk  data: {"text": "今天..."}
  event: chunk  data: {"text": "推荐..."}
  event: done   data: {"content_id": "xxx", "word_count": 800}
```

#### 数据看板查询接口
```
GET /api/v1/analytics/dashboard
Query: { account_id, platform, time_range, metrics[] }
Response: { metrics: { views, likes, fans }, trend: [...], alerts: [...] }
```

#### 发布任务接口
```
POST /api/v1/publish/tasks/batch
Request: { tasks: [{ content_id, platform, scheduled_time }] }
Response: { task_ids: [...], estimated_publish_times: [...] }
```

---

## 11 第三方平台集成设计

### 11.1 平台适配器模式

所有平台集成通过**适配器模式**统一封装，新平台接入仅需实现接口：

```
接口定义（PlatformAdapter）：
├── authenticate(credentials) → OAuth Token
├── publishContent(content, config) → PostId
├── fetchMetrics(postId, dateRange) → Metrics
├── fetchComments(postId, page) → Comments[]
├── replyComment(commentId, text) → void
├── fetchFansData(accountId) → FansProfile
└── fetchHotTopics(category) → Topic[]

已实现适配器：
├── DouyinAdapter      # 抖音（字节跳动开放平台）
├── XiaohongshuAdapter # 小红书（薯条/创作者平台接口）
├── WechatMPAdapter    # 微信公众号（微信开放平台）
├── KuaishouAdapter    # 快手开放平台
├── BilibiliAdapter    # B 站开放接口
└── VideoChannelAdapter# 视频号（微信视频号）
```

### 11.2 平台接口容错设计

```
平台 API 调用容错链路：
                          ┌─ 主接口调用
请求 → 适配器 → 重试机制 ─┤  (超时 3s，重试 3 次)
                          └─ 降级策略（返回缓存数据 + 告警）

接口变更应对：
├── 接口监控：每 5 分钟 Ping 各平台接口健康状态
├── 告警通知：接口异常立即推送运维告警
└── 快速适配：适配器设计支持热更新，不停机修复接口变更
```

### 11.3 数据采集频率控制

| 平台 | 采集频率 | 采集内容 | 限制处理 |
|------|----------|----------|----------|
| 抖音 | 实时（Webhook）+ 每 5min 补充 | 播放、点赞、评论、粉丝 | 官方 Webhook 优先，轮询兜底 |
| 小红书 | 每 15min | 阅读、点赞、收藏、评论 | 错峰请求，随机延迟 |
| 公众号 | 每 30min（数据 T+1）| 阅读、分享、取关 | 仅官方接口，不爬虫 |
| 快手 | 每 10min | 播放、互动、粉丝 | 批量接口优先 |
| B 站 | 每 15min | 播放、弹幕、投币 | 接口频率限制做令牌桶 |

---

## 12 安全架构设计

### 12.1 身份认证与授权

```
认证流程：
用户登录 → API 网关
               │
               ├── 普通登录：账号密码 → BCrypt 校验 → 签发 JWT
               │
               ├── OAuth 2.0：微信/企微/抖音第三方登录
               │
               └── 二次验证（高敏感操作）：TOTP / 短信验证码

JWT 设计：
{
  "sub": "user_id",
  "team_id": "xxx",
  "roles": ["editor"],
  "permissions": ["content:write", "analytics:read"],
  "exp": 3600,       // 1h 过期
  "refresh": true    // Refresh Token 24h
}
```

### 12.2 细粒度 RBAC 权限模型

```
角色定义：
├── 超级管理员（admin）    → 全部权限
├── 运营管理员（op_admin） → 内容 + 用户 + 数据全读写
├── 编辑（editor）         → 内容读写，数据只读
├── 运营（operator）       → 用户运营、发布调度
├── 数据分析师（analyst）  → 数据看板只读
└── 自定义角色             → 管理员任意组合权限

权限粒度：
资源：content / topic / analytics / publish / user / monetize / team
操作：create / read / update / delete / publish / export

示例：editor 角色权限
  content:create, content:read, content:update
  topic:read
  analytics:read（仅本账号数据）
```

### 12.3 数据安全

| 安全措施 | 实现方案 |
|----------|----------|
| 传输加密 | HTTPS（TLS 1.3），WebSocket over WSS |
| 存储加密 | MySQL 透明加密（TDE），敏感字段 AES-256 加密 |
| OAuth Token 存储 | Redis 加密存储，Token 脱敏日志 |
| 数据隔离 | 多租户数据行级隔离（account_id 强制过滤） |
| 操作审计 | 所有写操作记录操作日志（ES 存储，不可删除）|
| 数据脱敏 | 日志中手机号、邮箱自动脱敏 |
| 防 SQL 注入 | ORM 框架（MyBatis-Plus）参数化查询 |
| 防 XSS | 前端 DOMPurify 输入过滤 + 后端输出转义 |

### 12.4 内容安全多级检测

```
内容发布前检测链：

输入内容
    │
    ▼
第一级：本地敏感词库（毫秒级，前端预检）
    │ [疑似违规]
    ▼
第二级：阿里云内容安全 API（文本/图片/视频审核）
    │ [违规]  [通过]
    ▼         ▼
人工复审    第三级：原创度检测（多模型互检）
                │ [原创度 < 60%]  [通过]
                ▼                  ▼
           优化建议             版权风险检测
                                   │
                               [有风险] → 提示修改
                               [无风险] → 进入发布队列
```

---

## 13 部署架构设计

### 13.1 Kubernetes 集群部署拓扑

```
┌─────────────────────────────────────────────────────────────┐
│                   阿里云 ACK 集群                            │
│                                                             │
│  ┌──────────────────┐  ┌──────────────────────────────────┐ │
│  │  Ingress 层       │  │  Monitoring 命名空间              │ │
│  │  Nginx Ingress   │  │  Prometheus / Grafana / Jaeger    │ │
│  └────────┬─────────┘  └──────────────────────────────────┘ │
│           │                                                  │
│  ┌────────▼────────────────────────────────────────────┐    │
│  │            业务服务命名空间（wemedia-prod）            │    │
│  │                                                      │    │
│  │  topic-engine  (3 副本)  content-gen     (5 副本)    │    │
│  │  analytics     (3 副本)  data-collector  (3 副本)    │    │
│  │  publish-sched (2 副本)  notify-svc      (2 副本)    │    │
│  │  ai-engine     (3 副本)  user-profile    (3 副本)    │    │
│  │  其他微服务... (2+ 副本)                              │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │            数据服务命名空间（wemedia-data）           │    │
│  │  MySQL 主从集群  │  MongoDB ReplicaSet              │    │
│  │  Redis Cluster  │  ClickHouse 集群                 │    │
│  │  Kafka 集群      │  Elasticsearch 集群              │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │         AI 服务命名空间（wemedia-ai）                │    │
│  │  ml-prediction-svc (Python FastAPI, GPU 节点)       │    │
│  │  content-check-svc (内容合规检测, 独立伸缩)          │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 13.2 HPA 弹性扩缩容配置

```yaml
# 内容生成服务 HPA 示例
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: content-gen-hpa
spec:
  scaleTargetRef:
    name: content-gen
  minReplicas: 3
  maxReplicas: 20
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Pods
    pods:
      metric:
        name: ai_request_queue_length  # 自定义 AI 任务队列长度指标
      target:
        type: AverageValue
        averageValue: 50
```

### 13.3 多环境部署策略

| 环境 | 说明 | 部署方式 |
|------|------|----------|
| dev | 开发环境 | 单副本，共享 K8s 命名空间 |
| staging | 预发布环境 | 与生产同配置，独立集群 |
| prod | 生产环境 | 多副本，多可用区，HPA 弹性伸缩 |
| prod-cn | 国内生产 | 阿里云杭州 + 北京双区域 |
| prod-global | 海外扩展 | AWS Singapore（按需）|

---

## 14 高可用与容灾设计

### 14.1 高可用架构

```
多可用区部署（双活）：

杭州可用区A          杭州可用区B
┌──────────────┐    ┌──────────────┐
│ K8s节点组 A  │    │ K8s节点组 B  │
│ 所有微服务   │◄──►│ 所有微服务   │
│（各50%流量） │    │（各50%流量） │
└──────┬───────┘    └──────┬───────┘
       │                   │
       └──────────┬────────┘
                  │
        ┌─────────▼─────────┐
        │  MySQL 主从同步    │
        │  主节点（区A）     │
        │  从节点（区B）     │
        └───────────────────┘
```

### 14.2 关键链路高可用保障

| 链路 | 高可用措施 |
|------|-----------|
| 内容发布链路 | 发布任务持久化（MySQL），失败自动重试（3次），人工介入队列 |
| AI 生成链路 | 多模型备选自动切换，超时 30s 降级返回模板内容 |
| 数据采集链路 | 采集失败写入死信队列，人工处理告警；缓存上一次有效数据 |
| 通知推送链路 | 多渠道冗余（站内 + 短信 + 企微），消费幂等处理 |
| 用户鉴权链路 | JWT 无状态，Redis 仅存 黑名单（更快验证）|

### 14.3 灾备策略

| 数据类型 | 备份频率 | 保留周期 | 恢复目标 |
|----------|----------|----------|----------|
| MySQL 核心数据 | 全量每日 + 增量每 5min | 30 天 | RPO ≤5min，RTO ≤30min |
| MongoDB 内容数据 | 全量每日 | 14 天 | RPO ≤1h |
| ClickHouse 分析数据 | 全量每周 | 90 天 | RPO ≤1 天（可重算）|
| 用户上传素材（OSS）| 跨区域实时同步 | 永久 | RPO = 0 |

---

## 15 性能优化设计

### 15.1 响应时间优化（目标 ≤2s）

| 优化手段 | 应用场景 |
|----------|----------|
| **多级缓存** | L1: 本地 Caffeine 缓存（JVM 内，<1ms）→ L2: Redis 集群（≤5ms）→ L3: DB |
| **数据库读写分离** | MySQL 主库写，从库读（分析查询走 ClickHouse）|
| **分页 + 游标** | 大数据量列表接口使用游标分页，避免深度分页 |
| **并行聚合** | BFF 层多服务调用使用 CompletableFuture 并发执行 |
| **CDN 加速** | 静态资源、素材文件走 CDN（阿里云 CDN）|
| **数据预计算** | 数据看板核心指标定时预计算写入 Redis，查询直接读缓存 |
| **数据库索引优化** | 联合索引覆盖高频查询场景，避免全表扫描 |

### 15.2 AI 生成性能优化

| 优化手段 | 说明 |
|----------|------|
| 流式输出（SSE）| 用户不等待全部生成，边生成边展示，感知速度提升 80% |
| Prompt 缓存 | 相同参数的 Prompt 结果缓存（TTL=10min），避免重复调用 |
| 请求批处理 | 低优先级 AI 任务批量合并调用，降低 Token 消耗成本 |
| 模型分级路由 | 简单任务走轻量模型（成本低、速度快），复杂任务走高质量模型 |
| 异步处理 | 非实时任务（复盘报告、预测分析）走 Kafka 异步队列处理 |

### 15.3 大数据量处理优化（PB 级）

```
ClickHouse 查询优化：
├── 合理的分区键（按月分区）
├── 物化视图预聚合（日/周/月维度）
├── 本地表 + 分布式表（clickhouse-cluster）
├── 冷热数据分层存储（SSD 热数据 + HDD 冷数据）
└── 查询限流（重查询走异步，轻查询同步返回）

Kafka 吞吐优化：
├── 分区数量 = 消费者线程数（数据采集 Topic 12 分区）
├── 批量消费（batch.size=1MB，linger.ms=50ms）
└── 压缩（snappy 压缩，减少 50%+ 网络传输）
```

---

## 16 监控与可观测性设计

### 16.1 监控体系（三支柱）

```
┌────────────────────────────────────────────────────────┐
│                    可观测性三支柱                        │
│                                                        │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Metrics   │  │    Tracing   │  │   Logging    │  │
│  │             │  │              │  │              │  │
│  │ Prometheus  │  │  SkyWalking  │  │  ELK Stack   │  │
│  │ + Grafana   │  │  (分布式链路 │  │  (结构化日志 │  │
│  │             │  │   追踪)      │  │   + 全文检索)│  │
│  └─────────────┘  └──────────────┘  └──────────────┘  │
└────────────────────────────────────────────────────────┘
```

### 16.2 核心监控指标

| 监控维度 | 关键指标 | 告警阈值 |
|----------|----------|----------|
| **API 性能** | P99 响应时间、错误率、QPS | P99 > 2s 告警 |
| **AI 引擎** | 模型调用成功率、Token 消耗量、平均延迟 | 成功率 < 95% 告警 |
| **数据采集** | 平台接口成功率、采集延迟、消息积压量 | 积压 > 10000 告警 |
| **发布调度** | 发布成功率、任务延迟执行率 | 成功率 < 98% 告警 |
| **数据库** | MySQL 连接池使用率、慢查询数、主从延迟 | 主从延迟 > 5s 告警 |
| **Kafka** | 消息积压量、消费延迟、磁盘使用率 | 积压超阈值告警 |
| **K8s** | Pod CPU/内存使用率、OOM 次数 | CPU > 80% 触发扩容 |

### 16.3 业务监控看板

除基础设施监控外，系统提供业务层实时监控：

```
业务监控看板（Grafana）：
├── 内容生成量趋势（日/周）
├── AI 模型使用分布（各模型调用占比）
├── 各平台发布成功率
├── 活跃用户数与 DAU/MAU
├── 预警触发频率与处理率
├── 商单转化漏斗
└── 变现收入实时统计
```

---

## 17 架构演进路线

### 17.1 分阶段交付计划

#### 第一阶段：MVP（1-3 个月）

目标：核心业务流程跑通，支撑 100 用户

- 核心微服务：选题引擎、内容生成、发布调度、数据采集（仅抖音 + 小红书）
- AI 接入：GPT-4o（文本生成）+ 阿里云内容安全（合规检测）
- 存储：MySQL + Redis + MongoDB（基础配置）
- 前端：Web 端核心页面（选题工作台 + 内容创作台 + 基础数据看板）
- 部署：单集群，手动扩容

#### 第二阶段：完整功能（4-6 个月）

目标：全功能上线，支撑 1000+ 用户

- 完善所有微服务，接入 6 个平台适配器
- 接入多模型（Gemini、文心一言、通义千问）+ 图像生成
- 用户画像与精准互动模块全量上线
- 数据分析升级：ClickHouse + 预测分析（ML 服务）
- 工作流引擎接入，支持用户自定义自动化规则
- 前端 H5/小程序上线

#### 第三阶段：规模化（7-12 个月）

目标：5000+ 并发用户，PB 级数据

- 多可用区双活部署
- HPA 弹性扩缩容全面配置
- 数据分层存储完善（冷热分层、跨区域备份）
- 向量数据库接入（语义检索升级）
- 服务网格（Istio）全面落地
- 海外扩展（AWS 部署）

### 17.2 架构关键决策记录（ADR）

| 决策 | 选择 | 弃选 | 理由 |
|------|------|------|------|
| 后端框架 | Spring Boot 3（Java）| Go 单体 | 微服务生态更完整，团队熟悉度高 |
| 分析数据库 | ClickHouse | Hive / Spark | 实时查询性能更优，运维成本较低 |
| 工作流引擎 | Flowable | Activiti / 自研 | 成熟稳定，BPMN 2.0 标准，社区活跃 |
| 前端框架 | React + AntD | Vue3 + ElementUI | 生态更大，TypeScript 支持更好 |
| AI 接入框架 | LangChain4j | 自研 | 成熟框架避免重复造轮子 |
| 消息队列 | Kafka | RocketMQ | 大数据生态配合更好，高吞吐 |

---

*本文档为 AI 智能自媒体系统架构设计的基准版本，随系统演进持续迭代更新。*
